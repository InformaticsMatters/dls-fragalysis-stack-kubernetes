---
kind: PersistentVolumeClaim
apiVersion: v1
metadata:
  name: database
  namespace: {{ stack_namespace }}
spec:
  accessModes:
  - ReadWriteOnce
  storageClassName: '{{ database_vol_storageclass }}'
  resources:
    requests:
      storage: '{{ database_vol_size_g }}Gi'
