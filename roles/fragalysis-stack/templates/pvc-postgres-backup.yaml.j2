---
kind: PersistentVolumeClaim
apiVersion: v1
metadata:
  name: bu-pvc
  namespace: {{ stack_namespace }}
spec:
  accessModes:
  - ReadWriteOnce
  storageClassName: '{{ database_bu_vol_storageclass }}'
  resources:
    requests:
      storage: '{{ database_bu_vol_size_g }}Gi'
